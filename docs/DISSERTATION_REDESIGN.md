# 大论文重新设计：从状态标签到介入策略

## 0. 结论先行

当前项目不应继续围绕“30 秒 idle 是否能识别需要帮助”扩展。那条路线最多形成一个数据处理和候选检测小论文。

大论文的核心对象应改为：

> **在线编程中，系统如何根据不完整的行为证据，预测进展风险，学习合适的介入时机，并通过因果实验验证介入收益与打扰代价。**

这不是把 `Need / Interruptibility / Outcome` 做成三个分类器，而是一个连续的决策问题：

```text
行为和代码事件 H_t
        -> 进展/困难风险估计
        -> 是否值得介入、何时介入、介入多强
        -> wait / invite / hint / suppress
        -> 学生响应、任务结果、学习迁移
```

## 1. 三个阶段的正确含义

这里的三个阶段不是三个标签，而是三个不同的科学问题：

| 阶段 | 科学问题 | 主要估计对象 | 不能混淆 |
|---|---|---|---|
| 阶段一：Need | 当前行为是否显示进展风险或帮助价值？ | latent progress / struggle risk | 不能把 idle 直接当作 Need |
| 阶段二：Interruptibility | 即使可能需要帮助，现在采取哪种行动的代价最小？ | intervention opportunity / policy | 不能把“需要帮助”直接等于“立即弹窗” |
| 阶段三：Outcome | 采取行动相对于等待是否真正改善结果？ | causal treatment effect / learning effect | 不能把自然恢复当作提示收益 |

`Need / Interruptibility / Outcome` 是这三个阶段中的测量变量，但论文的主线是从风险估计到策略决策再到因果验证。

## 2. 阶段一：进展风险与帮助价值建模

### 2.1 不再研究“是否空闲”，而研究“是否失去有效进展”

当前代码能可靠得到按键、时间间隔、退格、光标行列变化、运行/提交上下文和判题结果代理，但它还没有真正表示“进展”。阶段一的实质工作是构造一个**可观测的编程进展过程**。

建议定义三类进展信号：

1. **编辑进展**：代码长度、结构变化、局部修改是否产生稳定变化；
2. **调试进展**：失败结果是否改变、错误是否持续、无修改重复运行是否发生；
3. **任务进展**：提交/运行阶段是否从尝试进入验证，是否接近可接受结果。

早期只有行为日志时，可用文本键、退格、行列变化和代码回放近似；后续在线采集必须增加代码快照、运行事件、测试反馈和页面状态。

### 2.2 四种互补方法，不是堆模型

#### 方法 A：过程挖掘

把每个 session 表示成事件序列，挖掘：

```text
edit -> pause -> run -> fail -> small_edit -> run -> fail
```

与：

```text
edit -> pause -> inspect -> edit
```

等过程变体。研究问题是：哪些路径在不同题目和学生之间稳定出现，哪些路径后续更可能需要帮助？

产出不是一堆计数，而是**进展停滞的过程模式**。

#### 方法 B：隐半马尔可夫模型（HSMM）

用隐状态表示：

```text
productive_editing
deliberation
debugging
waiting_for_judge
stalled
away_or_unknown
```

与普通 HMM 的区别是显式建模状态持续时间。这样“停留 30 秒”不再是硬边界，而是状态持续时间分布的一部分。

关键验证：

- 状态持续时间是否比固定 idle 阈值更能解释后续行为；
- 去掉 duration 后是否明显下降；
- 跨用户、跨题目、跨时间是否仍然有效。

#### 方法 C：相对进展与个人基线

不要直接比较绝对秒数，而比较：

```text
当前停滞时间 / 该用户此前停滞基线
当前停滞时间 / 该题目同类 session 的分布
当前编辑效率 / 个人历史编辑效率
```

这可以形成学生级冷启动与个性化问题：固定阈值和个人相对阈值在相同提示预算下谁更稳定。

#### 方法 D：弱监督而非单标签监督

将“长 idle、重复失败、无修改运行、回退爆发、代码进展停滞”等视为不同标注函数，用弱监督模型估计潜在困难风险，并保留冲突和不确定性。

这样人工标签不再被假装成无误差 gold truth；它们用于校准和审计，而不是定义全部真值。

### 2.3 阶段一的实质性研究问题

> 相比固定 idle 规则，带有过程结构、状态持续时间、相对个人基线和多源弱监督的进展风险模型，能否在跨用户/跨题目测试中更早发现“有效进展即将停滞”的时刻？

### 2.4 阶段一的决定性实验

必须比较：

1. `idle >= 30s`；
2. 多事件规则并集；
3. 轻量监督模型；
4. HSMM/过程模型；
5. HSMM 去掉 duration 的消融；
6. 固定阈值 vs 个人/题目相对阈值。

指标不以单一 F1 为中心，而是：提前量、候选召回、跨用户 PR-AUC、校准、误提示率和每 session 候选负担。

## 3. 阶段二：介入机会与 Navigator 策略

### 3.1 研究对象从“适不适合打扰”变成“采取什么行动”

Interruptibility 不是一个静态标签。它应被建模为行动相关的机会成本：

```text
此刻 wait 的风险
此刻 invite 的风险
此刻 hint 的风险
此刻 suppress 的风险
```

同一个学生可能确实需要帮助，但立即给具体答案会破坏思考；低强度询问可能合适，而直接提示不合适。

### 3.2 三个有实质内容的方法

#### 方法 A：风险/生存模型

估计如果系统不介入，未来一段时间内：

```text
继续有效编辑
运行
提交
离开
```

各自的风险。不同动作的候选时刻由风险曲线确定，而不是由 30 秒固定切点确定。

#### 方法 B：从人类 Navigator 反推策略

开展小规模传统结对编程实验，记录 Navigator：

- 什么时候观察；
- 什么时候等待；
- 什么时候提问；
- 什么时候给方向；
- 什么时候选择不干预。

用序列建模或 inverse reinforcement learning 估计人类的介入策略，再比较 AI 是否能复现这些时机选择。

这使“参考传统 Navigator”变成可测量的策略建模，而不是功能表格。

#### 方法 C：风险约束的混合主动策略

系统每次只做四种动作之一：

```text
wait / invite / hint / suppress
```

策略目标：

```text
预期恢复收益 - λ × 预期打扰代价 - 调用成本
```

并受以下约束：

- 每 session 提示上限；
- P95 提示次数上限；
- 延迟上限；
- LLM 调用预算；
- 用户随时关闭和撤销。

这可以逐步实现为风险约束 contextual bandit；只有在线反馈足够后，才考虑 POMDP。

### 3.3 阶段二的实质性研究问题

> 在相同提示预算和延迟约束下，基于自然行为风险与人类 Navigator 时机模型的策略，能否比固定 idle 规则减少无效中断，同时保留帮助候选覆盖率？

### 3.4 阶段二的决定性实验

比较：

1. 固定 `idle >= 30s`；
2. 固定多规则策略；
3. 风险曲线策略；
4. 人类 Navigator 时机模型；
5. 风险策略去掉人类策略特征；
6. 风险策略去掉提示预算约束。

主指标是 benefit--disruption frontier，而不是单点 F1：

- 相同提示率下的有效恢复率；
- 相同恢复率下的误打扰率；
- 每 session 提示数和 P95；
- 延迟与成本；
- 学生接受、忽略和关闭比例。

## 4. 阶段三：介入效果与学习后果

### 4.1 现有离线 Outcome 只能做自然过程基线

当前的 30/60/120 秒继续输入、运行和提交，只能回答：

> 如果系统不提示，学生行为通常如何发展？

它不能回答：

> 如果系统提示，学生是否比不提示更好？

因此阶段三必须从描述性 Outcome 升级到因果 Outcome。

### 4.2 三层方法

#### 方法 A：候选级微随机实验

在满足安全条件的候选时刻随机分配：

```text
control：不提示
invite：低打扰询问
hint：用户主动确认后提供一级帮助
```

主要结果：

- 到下一次有效编辑的时间；
- 到下一次运行/提交的时间；
- 提示接受、忽略、关闭；
- 短期任务结果；
- 主观打扰和控制感。

#### 方法 B：异质处理效应

研究哪些人、哪些题目、哪些行为模式下提示有效，哪些情况下反而有害：

```text
ITE(history) = outcome(prompt) - outcome(wait)
```

可比较 uplift model、causal forest 或 doubly robust policy evaluation，但必须以随机数据为主要识别来源。

#### 方法 C：中介与长期结果

把“提示是否有效”拆成：

```text
提示 -> 点击/接受 -> 下一步行为 -> 任务结果 -> 后续迁移
```

长期实验再观察：

- 新题目迁移；
- 延迟后独立完成；
- 对 AI 的依赖；
- 求助能力和自我调节；
- 对 Navigator 的信任与控制感。

### 4.3 阶段三的实质性研究问题

> 在相同困难风险和相同提示预算下，风险约束 AI Navigator 相对于等待和固定规则，是否提高短期恢复，同时不增加无效中断和长期依赖？

## 5. 三阶段如何连成一篇论文

```text
RQ1：什么是“进展风险”？
     过程挖掘 + HSMM + 相对进展 + 弱监督

RQ2：什么时候、以什么强度介入？
     hazard + 人类 Navigator 反推 + 风险约束策略

RQ3：介入是否真的有用？
     微随机实验 + 异质效应 + 中介/迁移分析
```

三阶段之间有明确依赖关系：

```text
没有 RQ1，策略没有可靠状态依据；
没有 RQ2，RQ1 只是识别论文；
没有 RQ3，前两者只能说明系统会判断，不能说明系统有价值。
```

## 6. 现有项目哪些保留，哪些必须重写

### 保留

- `extract_clean_dataset.py` 的可恢复数据抽取；
- 严格 `event_time < trigger_time` 的因果窗口；
- `full_causal_candidates.csv` 的事件驱动候选框架；
- 多尺度窗口统计；
- session/user 分组评估；
- 冷却、候选负载和自然 Outcome 审计；
- 30 秒作为 baseline。

### 必须重写或降级

- `idle_30s` 不能作为唯一科学定义，只能作为一种候选类型；
- `keys_30s < 5/8` 不能解释成卡住真值；
- `build_prototype.py` 的 100 session、500 窗口只能作为开发样本，不能作为最终验证集；
- 现有 LLM 零样本判断不能作为正式结果；其 prompt 使用了最终提交代码和窗口之后的信息，存在未来泄漏；
- `judge` 如果是当前 solution 的最终结果，不能直接展示给窗口标注者；
- 当前人工标签不能作为唯一 gold truth，必须补候选外抽样和双人标注；
- `Outcome` 不能回写为 Need，也不能用自然恢复率证明干预有效。

## 7. 30 秒、规则和三类变量在新论文中的位置

| 项目 | 新论文中的位置 |
|---|---|
| 30 秒 | 固定规则 baseline 和复现实验条件 |
| idle | 进展风险模型的一个观测变量 |
| Need | 当前历史下的帮助价值/进展风险估计 |
| Interruptibility | 不同行动的中断成本和机会窗口 |
| Outcome | 自然过程或随机干预后的结果变量 |
| 规则 | 候选生成和安全保护层 |
| AI Navigator | 风险约束的介入策略，不是单纯文本生成器 |

## 8. 最小可行的大论文版本

如果时间和在线资源有限，最低限度也应完成：

1. 阶段一：过程模式 + HSMM/时间风险模型，证明比固定 idle 更能解释进展停滞；
2. 阶段二：风险约束策略，至少有 wait/invite/suppress 和提示预算；
3. 阶段三：候选级随机不提示对照，至少验证短期恢复和打扰代价；
4. 人类 Navigator 只做小规模功能级对照，不声称完全替代。

这已经比“定义三个标签、比较几个分类器”有实质性深度。

## 9. 应该放弃的路线

- 继续寻找一个神奇的 27/30/33 秒；
- 只增加键盘、停顿、退格等统计特征；
- 只把随机森林换成 Transformer；
- 只加 LLM/RAG 作为第二个判断器；
- 只用最终 AC 或自然恢复作为效果证明；
- 把 AI Navigator 描述成完整替代人类 Navigator。

## 10. 最终推荐题目

中文：

> **面向在线编程的进展风险建模与风险约束混合主动 AI Navigator 研究**

英文：

> **Progress-Risk Modeling and Risk-Constrained Mixed-Initiative AI Navigation for Online Programming**

核心贡献不再是某个阈值，而是一个可验证的机制：

> 从过程事件估计进展风险，结合人类 Navigator 的介入规律，在中断预算下选择行动，并用随机实验估计介入的真实收益与代价。

