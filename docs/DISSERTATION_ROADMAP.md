# 大论文路线：面向在线编程的状态感知 AI Navigator

## 1. 总体判断

当前项目已经形成一个有价值的小论文问题：

> 在严格不使用未来信息的前提下，从在线编程行为中识别“值得进入帮助确认阶段”的候选时刻。

这个问题本身不足以支撑完整大论文。大论文应把研究对象升级为一个**状态感知、风险约束、混合主动的 AI Navigator**：

```text
行为/代码/判题事件
    -> 学生编程状态估计
    -> 帮助需求估计 Need
    -> 可打扰性与中断风险估计 Interruptibility
    -> 干预策略：show / delay / suppress + 帮助强度
    -> 用户响应与学习结果
    -> 在线反馈和策略更新
```

不要把贡献写成“提出一个 30 秒 idle 规则”或“使用随机森林/LLM”。真正的研究问题是：

> AI 能否在不增加不必要中断的前提下，部分承担传统结对编程 Navigator 的观察、提问、导航和节奏管理功能？

## 2. 推荐主线

推荐题目方向：

> 面向在线编程的状态感知、风险约束混合主动 AI Navigator：行为建模、干预策略与结对编程比较

推荐主张分成三层，避免把检测、干预和学习效果混为一谈：

1. **可观测性层**：按键、代码变化、运行/提交和判题事件能否估计学生当前的编程状态？
2. **决策层**：在 Need 已存在时，系统能否选择合适的时机、频率和帮助强度？
3. **效果层**：这种干预是否比无主动帮助、简单 idle 规则或人工 Navigator 的部分功能更有效，且更少打扰？

## 3. 四个核心研究问题

### RQ1：状态建模

能否从多模态过程数据中区分 `active_editing`、`thinking_or_reading`、`checking`、`waiting_for_judge`、`struggling`、`away_or_unknown` 等状态？

方法层面可比较：

- 规则和逻辑回归基线；
- 隐马尔可夫模型/隐半马尔可夫模型，显式建模状态持续时间；
- 时序深度模型，如 TCN、Transformer 或带时间间隔的 RNN；
- 代码差异、行为节奏、提交上下文的多模态融合。

重点不是追求最复杂模型，而是证明“状态”比单一 idle 阈值更接近在线决策需要。评价必须按学生或 session 分组，并报告校准、PR-AUC、宏平均和状态持续时间误差。

### RQ2：帮助需求与可打扰性

Need 和 Interruptibility 是否是两个可分离、但相互影响的潜变量？

建议把输出改成：

```text
P(Need=1 | history)
P(Interruptibility=show_now | history)
P(state | history)
```

再由策略层决定 `show / delay / suppress`。人工标签保留 `uncertain`，并报告双人一致性；在线日志单独记录“系统判断”和“学生实际接受/拒绝”。这样可以分析：学生确实卡住但不适合马上打扰、没有明显卡住但主动求助、系统判断不确定等关键情形。

### RQ3：风险约束的干预策略

在离线数据中预测“有帮助”不等于应该弹窗。策略要同时优化帮助收益和中断代价：

```text
utility = expected_help_gain
          - lambda * expected_disruption
          - cost_per_prompt
```

可按研究深度分三档：

1. **确定性策略**：冷却、候选去重、判题等待保护、失焦保护、提示上限；
2. **生存分析/风险模型**：估计继续输入、运行、提交或离开的时间风险；
3. **上下文 bandit / POMDP**：在安全约束下选择何时提示、提示强度和等待时间。

大论文最适合的技术升级是“风险约束策略”，而不是直接宣称端到端替代 Navigator。策略至少要支持：延迟、静默、低强度邀请、分级提示、用户主动求助和撤销。

### RQ4：与传统结对编程的关系

把传统 Navigator 的工作拆成可计算功能，而不是笼统比较“人 vs AI”：

| 传统 Navigator 功能 | AI 可承担的部分 | 当前系统对应物 |
|---|---|---|
| 观察 Driver 的操作节奏 | 检测停顿、回退、重复修改、运行/提交循环 | 多尺度行为窗口 |
| 发现局部卡点 | 汇总近期代码差异、错误结果和操作循环 | Need 估计 |
| 提问澄清 | 生成非阻塞帮助邀请或澄清问题 | `show` 与帮助入口 |
| 提供下一步导航 | 给出局部方向、检查点或搜索建议 | 分级帮助内容 |
| 管理节奏和轮换 | 冷却、延迟、提示上限和用户控制 | Interruptibility 策略 |
| 共享心智模型 | 解释证据并让用户确认 | evidence + 用户反馈 |

不能声称 AI 完全替代人类 Navigator。更稳妥的实验问题是：AI 是否能可靠承担其中的“观察、时机判断和低强度提问”子集，以及哪些任务仍需要人类共同理解和协商。

## 4. 数据与实验分层

### 4.1 离线层：过程建模

使用当前已清洗的 OJ 数据，严格执行：

- 所有触发特征来自 `event_time < trigger_time`；
- `outcome_*`、下一次提交和长期结果只能用于事后评价；
- 按学生划分训练/验证/测试，禁止随机拆同一 session 的窗口；
- 同时报告窗口级、session 级和每 session 误提示次数；
- 额外抽检规则未触发窗口，估计候选召回盲区。

离线主要回答“看得准不准”和“代价大不大”，不回答“提示是否造成学习收益”。

### 4.2 影子层：真实在线分布

前端真实运行但不显示提示，记录：

- 候选产生、保护规则抑制、LLM/模型判断和延迟；
- 继续输入、运行、提交、失焦、关闭等自然行为；
- 每 session 候选数、冷却命中率和候选重复率。

影子层用于验证离线分布是否与真实交互一致，并发现数据库中没有的状态，如阅读题目、切换窗口、复制粘贴和网络等待。

### 4.3 在线层：随机对照

只在满足安全条件的候选中随机分配：

- `control`：不展示；
- `low_intrusion`：非阻塞帮助邀请；
- 可选 `scaffold`：用户主动点击后提供分级帮助。

主结果包括恢复有效输入时间、下一次运行/提交时间、提示接受/忽略/关闭、主观打扰程度和后续判题表现。只有完成这个阶段，才可以讨论干预效果。

### 4.4 结对编程比较层

设计小规模、受控的三条件实验：

1. 单人编程，无主动帮助；
2. AI Navigator；
3. 人类 Navigator 或基于传统结对协议的人工对照。

任务、顺序和难度需要 counterbalance。除了正确率，还要测完成时间、求助次数、无效中断、认知负荷、控制感、信任、依赖和迁移任务表现。人类条件重点记录 Navigator 何时观察、何时提问、何时建议，以便和 AI 的事件日志做功能级比较。

## 5. 技术栈建议

### 基础层

- 事件流规范化与 session 切分；
- 代码 diff/AST 或轻量语义变化；
- 判题结果、提交间隔和等待状态；
- 时间间隔特征、多尺度统计和变点检测。

### 建模层

- 状态估计：HSMM 或带时间间隔的时序模型；
- 结果预测：离散时间生存模型/competing risks，分别预测继续编辑、运行、提交、离开；
- 融合模型：行为编码器 + 代码差异编码器 + 提交上下文；
- 不确定性：概率校准、选择性预测、`unknown` 拒答；
- 策略层：规则保护 + contextual bandit/POMDP 的风险约束决策。

### 系统层

- 事件驱动候选生成；
- 确定性安全过滤；
- 低延迟本地模型优先，LLM 只处理高价值候选；
- 固定 JSON 输出、证据链和模型版本；
- 全量记录提示生命周期，支持回放和审计。

## 6. 可形成的论文贡献

最少应形成以下四项贡献：

1. **数据贡献**：一个严格因果、事件驱动、可复现的在线编程行为数据处理和窗口构建流程；
2. **建模贡献**：把编程过程表示为带持续时间和不确定性的潜在状态，而不是单一卡顿标签；
3. **决策贡献**：提出同时考虑帮助收益、打扰风险、调用成本和用户控制的混合主动策略；
4. **实证贡献**：通过离线、影子、随机对照和结对编程比较，说明 AI Navigator 能承担哪些功能、在哪些情况下失败以及是否改善真实交互。

如果只能完成前三项，论文应定位为“状态感知主动帮助原型及离线/影子验证”；只有完成随机对照，才可以把在线干预效果作为核心贡献。

## 7. 必须正面处理的硬伤

- `previous_submission_result` 不能默认等于学生当时已看到的结果；若无法证明返回时间，应使用安全时间窗并明确是代理变量；
- 单一标注者和整题统一标签会夸大模型表现，必须补双人标注、边界样本和 session 级统计；
- `idle` 同时包含思考、阅读、等待和离开，必须用状态标签、页面/运行上下文或在线反馈拆分；
- 后续行为可以是 Outcome，但不能回写成触发时刻的 Need 真值；
- 不能用最终 AC 率证明提示有效，必须有不展示对照；
- 不能把 LLM 评审当作 ground truth，也不能让 LLM 只看规则命中的窗口后声称解决漏报；
- 不能把复杂模型本身当创新，必须和可解释基线、成本、延迟、误打扰和跨学生泛化比较；
- 学生代码和行为数据应留在受控环境，公开仓库只放代码、文档、脱敏样例和统计结果。

## 8. 推荐实施顺序

### 阶段 A：小论文闭环

完成双人 Need/Interruptibility 标注、候选外抽检、分组评估、冷却与在线日志协议。

### 阶段 B：状态建模

引入 `observed_state` 和 `uncertain`，比较规则、HSMM、时序模型和多模态融合；报告校准和 session 级泛化。

### 阶段 C：风险约束 Navigator

建立候选、保护、延迟、分级帮助和反馈回流；在影子模式验证候选分布、延迟和成本。

### 阶段 D：在线效果与结对比较

进行候选级随机对照，再进行 AI Navigator、人类 Navigator 和无主动帮助的受控比较；最后分析学习迁移、控制感、依赖和失败模式。

## 9. 最终建议

最稳妥且技术深度足够的主线是：

> “用多模态过程数据估计学生编程状态，再用风险约束的混合主动策略实现部分 Navigator 功能，并通过随机对照和人类结对编程比较验证其帮助收益与中断代价。”

这条路线既能承接当前 OJ 数据和小论文结果，也能自然连接传统 Driver–Navigator；它的难点在于状态不完全可观测、标签有噪声、干预会改变后续行为、以及收益和打扰之间的因果权衡。这些才是大论文值得深入的部分。

## 10. 离线优先修订（2026-09-14）

路线应按递进证据组织，而不是同时承诺状态识别、策略优化、学习收益和 Navigator 替代：

```text
标签/候选审计
    -> 跨口径、跨用户的状态与需求证据
    -> 不确定性与 abstention
    -> 风险约束策略的影子运行
    -> 随机在线效果
    -> AI/人类 Navigator 功能级比较
```

当前优先结果统一保存在 `labeling/data/offline_research/`，实验入口为 `labeling/offline_research_eval.py`。该审计显示：

- 最终高召回标签上规则 F1 约 0.70，但按 session 的 95% 区间约为 0.63–0.76；
- 保守复核标签上规则 F1 约 0.39，证明标签口径会显著改变结论；
- 三套标签的 session 全同标签比例约 45%–76%，不能把窗口当成独立样本；
- 删除 idle/停顿特征后模型仍保留一定排序能力，但性能和校准下降，当前高分同时包含真实行为信号与标签循环；
- 全量事件候选 172,622 条，10 分钟冷却后约 56,153 条，候选后的自然恢复率 30/60/120 秒约为 35.0%/53.2%/70.5%，不能解释为提示收益。

因此大论文离线阶段的正式主张应限制为：严格因果候选构造、操作性标签下的可解释基线、跨口径稳定性审计、选择性预测和候选运营代价。只有补齐候选集外双人标注与在线随机对照后，才升级到“状态感知 Navigator 策略有效”的表述。详细实验矩阵见 `OFFLINE_RESEARCH_PLAN.md`。
