# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 排除，不会被提交）
# 仅在需要连接数据库或调用 LLM 时使用。

# 数据库密码（export/filter/build/eval/replay 等脚本使用）
OJ_DB_PASSWORD=

# LLM 二次复核的 API Key（labeling/llm_judge.py 使用）
OJ_LLM_API_KEY=
